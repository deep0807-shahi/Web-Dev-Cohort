function isLongerThanFive(str) {
  return str.length > 5 ? "String is longer than 5 characters" : "String is 5 or fewer characters";
}
console.log(isLongerThanFive("Hello!"));       


// Output: "String is longer than 5 characters"